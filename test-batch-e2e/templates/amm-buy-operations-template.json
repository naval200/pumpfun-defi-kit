[
  {
    "type": "buy-amm",
    "id": "amm-buy-1",
    "description": "Buy HUE tokens via AMM for user-1",
    "sender": "{{USER_1_WALLET}}",
    "params": {
      "poolKey": "{{POOL_KEY}}",
      "amount": 5000000,
      "slippage": 100,
      "tokenMint": "{{MINT}}"
    }
  },
  {
    "type": "buy-amm",
    "id": "amm-buy-2",
    "description": "Buy HUE tokens via AMM for user-2",
    "sender": "{{USER_2_WALLET}}",
    "params": {
      "poolKey": "{{POOL_KEY}}",
      "amount": 7500000,
      "slippage": 100,
      "tokenMint": "{{MINT}}"
    }
  },
  {
    "type": "buy-amm",
    "id": "amm-buy-3",
    "description": "Buy HUE tokens via AMM for user-3",
    "sender": "{{USER_3_WALLET}}",
    "params": {
      "poolKey": "{{POOL_KEY}}",
      "amount": 10000000,
      "slippage": 100,
      "tokenMint": "{{MINT}}"
    }
  },
  {
    "type": "buy-amm",
    "id": "amm-buy-4",
    "description": "Buy HUE tokens via AMM for user-4",
    "sender": "{{USER_4_WALLET}}",
    "params": {
      "poolKey": "{{POOL_KEY}}",
      "amount": 12500000,
      "slippage": 100,
      "tokenMint": "{{MINT}}"
    }
  },
  {
    "type": "buy-amm",
    "id": "amm-buy-5",
    "description": "Buy HUE tokens via AMM for user-5",
    "sender": "{{USER_5_WALLET}}",
    "params": {
      "poolKey": "{{POOL_KEY}}",
      "amount": 15000000,
      "slippage": 100,
      "tokenMint": "{{MINT}}"
    }
  }
]
